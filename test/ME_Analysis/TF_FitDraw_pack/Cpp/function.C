/*
 *  function.C
 *  
 *
 *  Created by Arnaud Pin on 9/06/09.
 *  Copyright 2009 __MyCompanyName__. All rights reserved.
 *
 */

// DOUBLE GAUSSIAN FORM -----------------------------------------------------

		double p1 = a0 + a1 * pt + a2 * sqrt(pt);
		double p2 = a3 + a4 * pt + a5 * sqrt(pt) ;
		double p3 = a6 + a7 * pt + a8 * sqrt(pt);
		double p4 = a9 + a10 * pt + a11 * sqrt(pt);
		double p5 = a12 + a13 *pt + a14 * sqrt(pt);
		
/*		
		double p1 = p[0] + p[1] * pt + p[2] * sqrt(pt);
		double p2 = p[3] + p[4] * pt + p[5] * sqrt(pt);
		double p3 = p[6] + p[7] * pt + p[8] * sqrt(pt);
		double p4 = p[9] + p[10] * pt + p[11] * sqrt(pt);
		double p5 = p[12] + p[13] *pt + p[14] * sqrt(pt);
*/

------------------------------------------------------------------------------