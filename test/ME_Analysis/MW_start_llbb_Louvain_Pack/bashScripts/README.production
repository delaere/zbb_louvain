#to do one time
source CreateDir.sh # create directory for each process
source cpFiles.sh # copy TFs, newsubmit code and MW card
source modifyCards.sh # modify cards, for higgs hypo. please enter the mass hypo. by hand in the MW card
source selectTFs.sh # select the TFs
#to do each time
source changeProcess.sh # change cards for the new process
source newLaunchJobs.sh # launch jobs with the new way, old way with launchJobs.sh
source collectJobs.sh # collect jobs (could be done by hand : faster)

#to use if necessary
rmJobs.sh #remove directories for a "run"